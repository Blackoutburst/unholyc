#include <uhcio.uhh>
#include "merge.uhh"
        
I32 main() {
    merge(5, 3) { (res) ->
        Log.msg("%d", res);
    };

    Foo.merge2(5, 5) { (res) ->
        Log.msg("2: %d", res);
    };

    return 0;
}
