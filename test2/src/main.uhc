#include <uhcio.uhh>
#include <uhcstd.uhh>
#include <uhcnet.uhh>

        
I32 merge(I32 a, I32 b, lambda (I32, I32) -> I32) {
    return func(a, b);
}

I32 main() {
    I32 result = merge(5, 3) { (aaa, bbbb) ->
        aaa + bbbb;
    };
    Log.msg("%d", result);

    return 0;
}
