#include <uhcio.uhh>

I32 merge(I32 a, I32 b, lambda (I32, I32) -> I32) {
    return func(a, b);
}

I32 main() {
    I32 result = merge(3, 4) { (a, b) ->
        a + b;
    }
    Log.msg("%d", result);

    return 0;
}
