#include <uhcio.uhh>
#include <uhcstd.uhh>

U0 unusedVar(I32 a, unused I32 b) {
    unused a;
} 

I32 main() {
    Log.info("Hello world");
    Mutex.It m = Mutex.create();
    Mutex.lock(m);
    Mutex.unlock(m);
    Mutex.destroy(m);
    
    return 0;
}
