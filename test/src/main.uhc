#include <uhcio.uhh>
#include <uhcstd.uhh>
#include <uhcmath.uhh>

U0 unusedExample(unused I32 a, I32 b) {
    unused b;
}

I32 main() {
    VectorF.It vec = { 0, 1, 2, 3 };

    Log.debug("%f %f %f %f", vec.x, vec.y, vec.z, vec.w);

    return 0;
}
