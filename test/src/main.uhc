#include <uhcio.uhh>
#include <uhcmath.uhh>

U0 unusedVar(I32 a, unused I32 b) {
    unused a;
} 

I32 main() {
    Log.info("Hello world");
    VectorF.It a = { 0, 0, 0, 0 };
    VectorF.It b = { 1, 0, 0, 0 };

    Log.info("%i" , VectorF.equals(a, b));

    VectorF.zero(b);

    Log.info("%i" , VectorF.equals(a, b));
    
    return 0;
}
