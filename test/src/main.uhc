#include <uhcio.uhh>
#include <uhcstd.uhh>

U0* asyncStuff(U0* arg) {
    U0 (*block)() = *(U0 (**)())arg;
    block();

    return NULL;
}

U0 io(lambda block() -> U0) {
    Thread.It t = Thread.start(asyncStuff, &block);
    Thread.join(t);
}

I32 main() {
    io() { 
        Log.msg("Other thread"); 
    };

    Log.msg("Main thread");

    return 0;
}
