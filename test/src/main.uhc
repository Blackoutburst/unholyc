#include <uhcio.uhh>
#include <uhcstd.uhh>
#include <uhcmath.uhh>

I32 main() {
    List.It<I32> list;

    List.add(list, 10);
    List.add(list, 20);
    List.add(list, 30);
    List.addAt(list, 99, 1);
    List.remove(list, 10);
    List.removeAt(list, 1);
    List.clear(list);

    return 0;
}
